const LIST_NAME = 'List for testing';
const ASSERT_DASHBOARD = 'SPACE';
const ASSERT_TASK = 'Task for testing';

module.exports = {
  LIST_NAME: LIST_NAME,
  ASSERT_DASHBOARD: ASSERT_DASHBOARD,
  ASSERT_TASK: ASSERT_TASK
};
